{
  "name": "@piston-labs/agent-coord-mcp-cloud",
  "version": "0.1.0",
  "description": "Lightweight MCP server for cloud agents - minimal dependencies for fast bootstrap",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "agent-coord-mcp": "./bin/agent-coord-mcp.js"
  },
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@upstash/redis": "^1.35.7",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "cloudAgent": {
    "note": "This package is for cloud agents only. It strips playwright, AWS SDKs, and other heavy deps.",
    "estimatedSize": "~15MB vs ~250MB full package",
    "bootTimeReduction": "~5-7 minutes saved"
  }
}
